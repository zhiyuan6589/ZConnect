<UserControl x:Class="ZConnect.Views.TcpServerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZConnect.Views"
             xmlns:models="clr-namespace:ZConnect.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="DataFormatValues"
                        MethodName="GetValues"
                        ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:DataFormatEnum"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.IsSharedSizeScope="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Margin="10" HorizontalContentAlignment ="Center" Content="Tcp Server" FontSize="20" FontWeight="Bold"/>

            <Label Grid.Column="0" Grid.Row="1" Margin="0 5 0 5" Content="Local IP:" />
            <TextBox Grid.Column="1" Grid.Row="1" Margin="10 5 0 5"
                 Text="{Binding Connection.LocalIp}" 
                 Style="{StaticResource VerticalCenterTextBox}"/>

            <Label Grid.Column="0" Grid.Row="2" Margin="0 5 0 5" Content="Local Port:"/>
            <TextBox Grid.Column="1" Grid.Row="2" Margin="10 5 0 5"
                 Text="{Binding Connection.LocalPort}" 
                 Style="{StaticResource VerticalCenterTextBox}"/>

            <Label Grid.Column="0" Grid.Row="7" Margin="0 5 0 5" Content="Status:"/>
            <TextBlock Grid.Column="1" Grid.Row="7" Width="80" Margin="10 5 0 5" 
                  Text="{Binding Status}" VerticalAlignment="Center"/>

            <Button  Grid.Column="0" Grid.Row="8" Width="80" 
                 Content="Start" Command="{Binding StartCommand}"/>
            <Button Grid.Column="1" Grid.Row="8" Width="80" 
                Content="Stop" Command="{Binding StopCommand}"/>
        </Grid>

        <Grid Grid.Column="1">
            <local:CommonPanelView DataContext="{Binding}"/>
        </Grid>
    </Grid>
</UserControl>
